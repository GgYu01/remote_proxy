# Proxy Configuration
# ==============================================================================

# --- System Configuration ---
# Swap size in GB (default: 2)
SWAP_SIZE_GB=2

# --- Proxy User Configuration ---
# Username and Password for SOCKS5/HTTP/Trojan
PROXY_USER=admin
PROXY_PASS=password123

# UUID for VLESS (Auto-generated if empty)
# Used for VLESS + Vision + Reality
VLESS_UUID=

# --- Network Configuration ---
# Base port for proxies.
# SOCKS5: BASE_PORT
# HTTP:   BASE_PORT + 1
# SS:     BASE_PORT + 2
# VLESS:  BASE_PORT + 3
# Trojan: BASE_PORT + 4
BASE_PORT=10000

# Shadowsocks Encryption (AEAD)
SS_METHOD=chacha20-ietf-poly1305
SS_PASSWORD=secret_ss_password

# --- Sing-box Configuration ---
# Memory limit for the container (e.g., 128M, 256M)
MEMORY_LIMIT=256M
